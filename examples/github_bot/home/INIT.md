Please initialize the environment as follows:
- Set the git's global `user.name` as: `Bot User`.
- Set the git's global `user.email` as: `bot@example.com`.
- Verify the `GITHUB_PAT` is correctly set in the environment.
